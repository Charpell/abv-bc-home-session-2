# Bootcamp-Http-Client-App

This app gets a user's profile on github and shows all the most recent repositary the user has.

This app makes use of axios http client library.

Run npm install axios to get started

Run node http-client.js 
